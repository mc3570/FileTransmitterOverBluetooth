<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.Mengchen_Zhang.filetransmitteroverbluetooth"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:name="applicationStaticListView.ApplicationStatic"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="MainActivity"
            android:label="@string/app_name" >
           	<intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="bluetooth_relevence.BluetoothActivity"
            android:label="@string/app_name"
            >
        </activity>
        <activity
            android:name="tabs_and_fragments.TabActivity"
            android:label="@string/app_name"
            >
        </activity>
        <activity
            android:name="sendingFileActivity.FileSendingActivity"
            android:label="@string/app_name"
            >
        </activity>
        <activity 
            android:name="openGL_CoverFlow.GalleyActivity"
            android:label="@string/app_name"
            >
        </activity>
    </application>

</manifest>
